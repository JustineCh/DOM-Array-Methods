The project aim is to use DOM Array Methods. The app shows peoople and their wealth.

In the project:
--  "Add User" button uses async await method and fetch random users
--  "Double Money" button uses map() method
--  "Show Millionaires" button uses filter() method
--  "Sort by Richest" button uses sort() method
--  "Calculate entaire Wealth" button uses reduce() method